Server.exe
	Request amount and names clients, create processes
Client.exe
	Get it's name by Server, get messages (another_client -> server -> client)
and send messages (reversed). [star topology]

syntaxys: to > message 
